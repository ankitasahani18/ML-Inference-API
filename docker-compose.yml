version: "3.9"

services:

  api:
    build: .
    ports:
      - "8000:8000"

    depends_on:
      - redis

  worker:
    build: .
    command: celery -A app.services.tasks worker --loglevel=info

    depends_on:
      - redis

  redis:
    image: redis:7